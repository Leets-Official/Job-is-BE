# 첫 프로덕션 배포에서 버그 세 개를 만난 날

Job-is 백엔드 인프라를 맡은 지 얼마 안 됐는데, 오늘 드디어 `main` 브랜치로 첫 prod 배포를 했다. 결론부터 말하면 성공했지만, 가는 길이 순탄하지만은 않았다. 배포 자체는 파이프라인이 다 만들어져 있어서 버튼 하나(정확히는 PR 머지 하나) 누르면 끝날 줄 알았는데, 실제로 눌러보니 숨어있던 버그가 세 개나 튀어나왔다. 오늘 겪은 걸 순서대로 정리해둔다.

## 배경

지금까지 dev 환경은 `develop` 브랜치에 푸시할 때마다 자동으로 배포되도록 CI/CD를 구성해뒀고, prod용 EC2와 파이프라인도 며칠 전에 다 세팅해뒀다. 근데 정작 `main`으로 실제 배포를 해본 적은 한 번도 없었다. 프론트가 랜딩페이지만이라도 배포됐다는 소식을 듣고, "지금이 첫 배포하기 좋은 타이밍이다" 싶어서 바로 진행했다.

순서는 이랬다:

1. prod용 `.env`에 프론트 배포 주소(CORS, OAuth 리다이렉트) 반영
2. `develop → main` PR 생성, 팀원 리뷰 받고 머지
3. GitHub Actions가 자동으로 테스트 → 이미지 빌드 → EC2 배포까지 진행

여기까지는 순조로웠다. 문제는 그 다음부터였다.

## 버그 1: 새 서버는 레지스트리 로그인이 안 되어있다

`main` 머지 후 `deploy-prod` job이 실패했다. 로그를 보니:

```
Image ghcr.io/leets-official/job-is-be:prod-latest Error error from registry: unauthorized
```

우리 컨테이너 이미지는 GHCR(GitHub Container Registry)에 Private으로 올라가는데, GitHub Actions 쪽은 `GITHUB_TOKEN`으로 알아서 인증되지만, **EC2 서버가 이미지를 pull할 땐 그 서버 자체가 별도로 로그인이 되어있어야 한다**는 걸 깜빡했다. dev 서버는 예전에 수동으로 로그인해둔 적이 있어서 몰랐는데, prod는 완전히 새 인스턴스라 이 로그인 자체가 아예 없었던 거다.

**해결**: EC2에 SSH로 들어가서 개인 액세스 토큰(PAT, `read:packages` 권한)으로 `docker login ghcr.io` 한 번 해주니 바로 풀렸다. 이 로그인 정보는 서버에 계속 남아있어서, 앞으로 같은 서버에 배포할 땐 다시 안 해줘도 된다. **다만 새 서버를 만들 때마다 이 스텝을 잊으면 안 된다** — 오늘처럼 첫 배포 때 반드시 걸리는 문제라서 체크리스트에 넣어둬야겠다.

## 버그 2: 아무도 몰랐던 dev의 조용한 크래시 루프

로그인 문제를 풀고 다시 배포했더니 이번엔 컨테이너가 뜨자마자 계속 재시작을 반복했다. `docker logs`로 들여다보니:

```
초기 회사 데이터 파일이 존재하지 않습니다. 경로: /app/database/data/companies.jsonl
GeneralException: 초기 데이터 파일 경로를 찾을 수 없습니다.
```

앱이 부팅할 때마다 자동으로 실행되는 초기화 로직이 있는데, 이게 크롤링해둔 회사/공고 데이터 파일(`.jsonl`)을 필수로 읽어서 DB에 채워넣는 역할을 한다. 근데 `Dockerfile`을 보니 빌드할 때 자바 소스(`src`)만 이미지에 담고 있었고, 이 데이터 파일이 들어있는 `database/` 폴더는 애초에 이미지에 넣은 적이 없었다. 파일 자체는 레포에 커밋되어 있는데, Docker 이미지 빌드 목록에 없었을 뿐이었다.

더 놀라웠던 건, 이 초기화 로직이 며칠 전 새로 머지된 기능이었다는 점이다. 즉 **그 이후로 dev 서버도 재배포될 때마다 계속 이 이유로 크래시하고 있었을 가능성이 높았다.** 확인해보니 실제로 dev도 502가 뜨고 있었다. GitHub Actions의 테스트는 계속 초록불이었는데, 그건 단위 테스트가 통과했다는 뜻이지 실제 배포된 서버가 살아있다는 뜻은 아니었던 거다. 배포 자동화가 잘 되어있다고 "안전하다"고 착각하면 안 된다는 걸 새삼 느꼈다.

**해결**: `Dockerfile`에 한 줄 추가.

```dockerfile
COPY database database
```

이슈 만들고 hotfix 브랜치 파서 고친 다음, develop에 먼저 반영해서 dev로 검증하고, 그다음 main에 반영해서 prod까지 재배포했다. dev/prod 둘 다 정상화됐다.

## 버그 3: nginx가 옛날 주소를 계속 붙잡고 있었다

두 번째 버그를 고치고 재배포했는데, 이번엔 502가 또 떴다. 근데 이번엔 컨테이너 자체는 멀쩡히 떠있었다(`docker ps`로 확인하니 재시작 없이 계속 `Up` 상태). 그런데도 nginx는 계속 502를 뱉었다.

원인은 nginx의 동작 방식에 있었다. nginx는 `proxy_pass http://api:8080;`처럼 컨테이너 이름으로 백엔드를 찾는데, **이 이름 → 내부 IP 변환을 프로세스가 시작할 때 딱 한 번만 하고 캐싱**해버린다. 근데 Docker Compose는 컨테이너를 재생성할 때마다 새 내부 IP를 발급한다. 배포 스크립트는 이미지가 바뀐 `api` 컨테이너만 새로 만들고 nginx는 안 건드리니까, nginx는 이미 사라진 옛 컨테이너의 IP를 계속 바라보고 있었던 거다. 즉 **api 이미지가 바뀌는 배포마다 항상 반복될 수밖에 없는 구조적인 문제**였다.

당장은 `docker compose restart nginx`로 넘어갔지만, 이건 배포할 때마다 사람이 기억해서 수동으로 해줘야 하는 땜빵이라 마음에 걸렸다. 그래서 근본적으로 nginx 설정 자체를 고쳤다:

```nginx
resolver 127.0.0.11 valid=10s;

location / {
    set $upstream_api api:8080;
    proxy_pass http://$upstream_api;
    ...
}
```

Docker Compose가 기본으로 제공하는 내장 DNS(`127.0.0.11`)를 nginx가 명시적으로 쓰도록 하고, `proxy_pass` 대상을 변수로 바꿔주면 nginx가 10초마다 알아서 다시 DNS를 조회한다. 이렇게 하니 컨테이너가 재생성돼도 nginx가 알아서 새 IP를 따라간다. dev/prod 둘 다 적용했고, 이제는 배포 후에 nginx를 수동으로 재시작할 필요가 없어졌다.

## 회고

세 버그 다 따로 보면 별로 대단한 원인은 아니다. 로그인 안 함, 파일 하나 안 담김, DNS 캐싱. 근데 셋 다 공통점이 있다면 **"로컬/dev에서 한 번 잘 되면 다음에도 잘 될 거라고 무의식적으로 믿었다"**는 거다. dev는 오래전에 수동으로 세팅해둔 게 계속 남아있어서 문제가 안 보였을 뿐이고, 새 환경(prod)에서는 그 숨겨진 전제들이 하나씩 다 드러났다.

그리고 두 번째 버그에서 배운 게 제일 컸다. CI가 초록불이라고 안심하면 안 된다는 것. 테스트 통과와 "배포된 서버가 실제로 살아있다"는 완전히 다른 이야기다. 앞으로는 배포 후에 실제 엔드포인트를 찔러보는 걸(지금은 수동으로 `/health` curl 하는 정도지만) 파이프라인에 자동으로 넣는 것도 고려해봐야겠다.

오늘 처리 못 한 것들은 다음으로 미뤘다:
- 도메인 구매 + HTTPS 적용 (지금은 프론트가 HTTPS라서 HTTP인 백엔드를 브라우저가 mixed content로 막고 있다. 서버단 API 자체는 정상)
- blue-green 무중단 배포 (오늘은 일부러 안 하기로 했다 — 첫 배포에 여러 변수를 한 번에 얹으면 문제 생겼을 때 원인 파악이 힘들어지니까)
- 일부 공고 데이터가 컬럼 길이 제한 때문에 저장 안 되는 자잘한 버그

인프라는 "한 번 세팅하면 끝"이 아니라 계속 발견하고 고쳐나가는 거라는 걸 오늘 제대로 느꼈다.
